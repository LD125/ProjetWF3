{% extends 'layout.html.twig' %}

{% block content %}
    <div class="row">
        
            <br><br><br>
            <h1>Contactez-nous</h1>
            <br>
            <h4 class="col-sm-6 col-sm-offset-3 text-center contact"><p>Si vous voulez nous suggérer un endroit qui mériterait notre attention,
                un produit qui vous tient à coeur, 
                ou que vous voulez simplement entrer en contact avec nous,
                n’hésitez pas à nous écrire.</p></h4>
           
       <div class="col-sm-6 col-sm-offset-3 well "> 
    
    {{ form_start(form) }}{# ouvre la balise form #}
     {{ form_row(form.email)}}
     {{ form_row(form.objet)}}
     {{ form_row(form.message)}}
                 
     <div class="form-btn-group">
         <button type="submit" class="btn btn-primary">Envoyer</button>
     </div>   
     {{ form_end(form) }}{# ferme la balise form #}
       </div>
    </div>
{% endblock %}{# empty Twig template #}