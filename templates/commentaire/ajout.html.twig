{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Poster un commentaire</h1>
    <div class="row">
    {{ form_start(form) }}{# ouvre la balise form #}
     
     {{ form_row(form.commentaire)}}
     {{ form_row(form.notecomment)}}
     
                 
     <div class="form-btn-group">
         <p><button type="submit" class="btn btn-lg btn-primary">Enregistrer</button></p>
     </div>   
     {{ form_end(form) }}{# ferme la balise form #}
</div>
     <div class="row">
     <a class="btn btn-lg btn-success" href="{{ path('app_restaraunt_index')}}">Retour aux restaurants</a>
</div>
{% endblock %}{# empty Twig template #}

